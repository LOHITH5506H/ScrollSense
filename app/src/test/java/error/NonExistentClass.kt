package error

/**
 * Temporary placeholder to satisfy references to @error.NonExistentClass()
 * created during earlier kapt stub generation. Remove both the usages and
 * this file when no longer needed.
 */
@Target(
    AnnotationTarget.CLASS,
    AnnotationTarget.FILE,
    AnnotationTarget.FUNCTION,
    AnnotationTarget.PROPERTY,
    AnnotationTarget.VALUE_PARAMETER
)
@Retention(AnnotationRetention.RUNTIME)
annotation class NonExistentClass

